/*
      //wait message from the server
      this.client.on('onChat', function(data:Object):void {
        trace(data.from, data.target, data.msg, (new Date()).getTime());
        //self.addMessage(data.from, data.target, data.msg, (new Date()).getTime());
        //$("#chatHistory").show();
        //if(data.from !== username)
        //  tip('message', data.from);
      });
      
      // update user list
      this.client.on('onAdd', function(data:Object):void {
        var user:String = data.user;
        //tip('online', user);
        //addUser(user);
      });
      
      // update user list
      this.client.on('onLeave', function(data:Object):void {
        var user:String = data.user;
        //tip('offline', user);
        //removeUser(user);
      });*/